	<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="shortcut icon" href="/logo.ico" type="image/x-icon" />
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <link href='https://fonts.googleapis.com/css?family=Mulish' rel='stylesheet'>
    <!-- Font Awesome JS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <!-- Upload CSS-->
    <link href="https://cdn.jsdelivr.net/gh/kartik-v/bootstrap-fileinput@5.2.5/css/fileinput.min.css" media="all" rel="stylesheet" type="text/css" />

    <style>
        body {
            background-image: url(/new-background.png);
            background-position: center;
            background-repeat: none;
            background-attachment: fixed;
            background-size: cover;
        }
        .container {
            width: 45%;
            height: 60%;
            background-color: rgba(255,255,255,0.1);
            border-radius: 20px;
            /* Center vertically and horizontally */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        .container-child {
            width: 100%;
            position: relative;
        }
        .logo {
            margin: 0;
            padding: 0;
            width: 45%;
        }
        .img-logo{
            width: 80%;
            height: 80%;
            object-fit: contain;
        }
        .content {
            width: 58%;
        }
        
        .content .text {
            font-size: 40px;
            font-weight: bolder;
            padding: 3rem 0 2rem;
        }
        .content .form{
            width: 75%;
            margin-bottom: 0.5rem;
        }
        .form-group{
            width: 100%;
            height: 4.5rem;
            margin-bottom: 1.5rem;
            padding: 0;
            position: relative;
        }
        .form-group img {
            position: absolute;
            top: 1rem;
            left: 0.65rem;
        }
        .image {
            width: 2rem;
            padding: 0.5rem;
            min-width: 2.5rem;
        }
        .input-field {
            width: 100%;
            height: 100%;
            padding: 0 4rem;
            border-radius: 40px;
            border: 2px solid #BDE6F1;
            outline: none;
            background-color: rgba(255,255,255,0.1);
        }
        .input-field:hover{
            border-color: rgba(255,255,255,0.7);
        }
        .input-field:focus{
            border: 3px solid #BDE6F1;
        }
        .form-group .label{
            position: absolute;
            left: 3rem;
            top: 1.5rem;
            font-size: 1rem;
            padding: 0 0.5rem;
            background-color: rgba(255,255,255,0.7);
            opacity: 0.8;
            transition: 
                top 200ms ease-in,
                left 200ms ease-in,
                font-size 200ms ease-in;
        }
        .submit{
            border: none;
            border-radius: 30px;
            font-size: 15px;
            height: 45px;
            outline: none;
            width: 100%;
            color: black;
            background: rgba(255,255,255,0.7);
            cursor: pointer;
            transition: .3s ;
        }
        .submit:hover{
            box-shadow: 1px 5px 7px 1px rgba(0, 0, 0, 0.2);
        }
        .input-field:focus + .label{
            top: -1rem; 
            font-size: 1.1rem;
            opacity: 1;
            color: #67cae3;
        }
        .input-field:not(:placeholder-shown).input-field:not(:focus) + .label {
            top: -1rem; 
            font-size: 1.1rem;
            opacity: 1;
            color: #67cae3;
        }
        .sub-text{
            font-size: 1.2rem;
            font-weight: 500;
            margin-bottom: 1rem;
            width: 65%;
            text-align: center;
        }
        .checkName{
            position: absolute;
            z-index: 10;
            width: 100%;
        }
        .danger_icon{
            padding-right: 10px;
        }
        
    </style>
    <title>Quên mật khẩu</title>
</head>
<body>
    <div class="container d-flex flex-row align-content-center">
        <div class="container-child d-flex flex-row ">
            <div class="logo d-flex justify-content-center align-content-center">
                <img src="/logo.png" alt="" class="img-logo align-self-center">
            </div>
            <div class="content d-flex flex-column align-content-center">
                <h2 class="text align-self-center">Quên Mật Khẩu</h2>
                <p class="sub-text align-self-center">Nhập tên tài khoản bạn muốn đổi mật khẩu</p>
                <form method="post" class="form align-self-center">
                    <div class="form-group">
                        <img src="/user.png" class="image">
                        <input type="text" id="username" name="username" class="input-field" placeholder="Tài khoản " autocomplete="off">
                        <div class="checkName"></div>
                    </div>
                    <div class="d-flex flex-row justify-content-center">
                        <input type="submit" formaction="/forgotPass" value="Đổi mật khẩu" class="submit align-self-center mt-4">
                    </div>                 
                </form>

                {{#if error}}
                    <div class="alert alert-danger d-flex align-items-center mt-1 alert-dismissible" role="alert"> 
                        <i class="fas fa-exclamation-triangle danger_icon"></i> 
                        <div> {{message}} </div>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {{/if}}
            </div>
        </div>
    </div>
</body>
<!-- Jquery -->
<script src="https://code.jquery.com/jquery-3.6.0.js"></script>

<!-- Popper.JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.2.2/Chart.min.js"></script>

<script>
    $(document).ready(function(){
        function alert(message, type, element) {
            $(`.check${element}`).html('<div class="alert alert-' + type + ' d-flex align-items-center mt-1 alert-dismissible" role="alert"> <i class="fas fa-exclamation-triangle"></i> <div> ' + message + ' </div><button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button></div>');
        }
        // Kiêm tra ràng buộc ở đăng nhập
        var checkName = 0;
        $('.submit').prop('disabled', true);
        $('#username').keyup(function() {
            if($(this).val().length != 0)
            {
                checkName = 1;
                $('.checkName').html('');
                if(checkName == 1)
                    $('.submit').prop('disabled', false);
            }
            else
            {
                checkName = 0;
                alert('Vui lòng điền tên tài khoản!', 'danger', 'Name');
                $('.submit').prop('disabled', true);
            }
        });
    });
</script>
</html>